@model IEnumerable<PAEDUCA.Models.AspectoAAC>

@{
    ViewBag.Title = "Gestión de AAC";
}

<h2>AAC</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAspectoACC }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAspectoACC }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAspectoACC })
        </td>
    </tr>
}

</table>
